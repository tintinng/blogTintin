spring:
  profiles: pro

  datasource:
    url: jdbc:mysql://localhost:3306/blog_tintin?useUnicode=true&characterEncoding=utf-8
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

logging:
  level:
    root: warn
    com.tintin: info
  file:
    name: log/blog-dev.log

server:
  port: 8081